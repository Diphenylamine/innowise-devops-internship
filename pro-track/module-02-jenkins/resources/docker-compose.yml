services:
  jenkins:
    build:
      context: ../../..
      dockerfile: pro-track/module-02-jenkins/resources/Dockerfile.jenkins
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home

volumes:
  jenkins_home: